# Product Specs (mirrored)

The 18 product epics for Fanfinity, **mirrored from `fanfinity/engage-api`** (the `specs/<area>/_epic.md` files there, which also back the GitHub issues labeled P0–P10). They're copied here so this marketing repo is **self-contained** — no marketing doc or skill references the sibling engage-api repo anymore.

These are the capability **source of truth** that `.agents/product-marketing.md` (and everything downstream) is grounded in.

**On copy they were cleaned** of cross-repo references: competitor source/image links removed, the `## Sources`/`## Screens` blocks dropped, and sub-spec links stripped (only the `_epic.md` overviews are mirrored, not the per-endpoint sub-specs).

Notes:
- Internal engineering reference only — under `.agents/`, so **excluded from the Jekyll build** (never published).
- Originally authored from competitor research; on copy, the competitor's brand name and its source/CDN references were replaced with Fanfinity's own. They describe Fanfinity's intended product.
- **To refresh:** re-copy `_epic.md` files from engage-api and re-run the same cleaning.
