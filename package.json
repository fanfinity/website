{
  "name": "website",
  "private": true,
  "scripts": {
    "data:build": "node scripts/build-integrations.mjs",
    "css:build": "npx tailwindcss -i ./src/main.css -o ./assets/css/main.css --minify",
    "css:watch": "npx tailwindcss -i ./src/main.css -o ./assets/css/main.css --watch",
    "build": "npm run data:build && npm run css:build && bundle exec jekyll build",
    "dev": "npm run data:build && (npm run css:watch & bundle exec jekyll serve --livereload --host 0.0.0.0)"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "tailwindcss": "^3.4.0"
  }
}
